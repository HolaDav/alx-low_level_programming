#include "main.h"
#include <stdio.h>

/**
 * main - print the name of it's function
 * @argc: Counts arguments
 * @argv: Holds the string values of argunemts
 *
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("The name if this file is: %s\n", argv[0]);
	return (0);
}
