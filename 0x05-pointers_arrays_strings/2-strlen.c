#include "main.h"

/**
 * _strlen - returns the length of a string.
 * @s: string variable
 *
 * Return: Lenght of string
 */

int _strlen(char *s)
{
	int len;

	s = "My world!";
	len = _strlen(s);
	return (len);
}
